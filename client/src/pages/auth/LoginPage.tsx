/**
 * LoginPage — Page Component
 *
 * Thin wrapper that renders the Login feature component.
 * Pages are responsible for layout context and route-level concerns.
 * The actual form logic lives in the Login component.
 */
export { Login as LoginPage } from '../../app/authentication/Login';
